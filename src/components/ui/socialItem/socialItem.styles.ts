import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const ItemBoxStyled = styled(Box)(({ theme }) => ({
  backgroundColor: "#323d48",
  padding: "10px",
  marginTop: "20px",
  borderRadius: "8px",
  color: "white",
}));
